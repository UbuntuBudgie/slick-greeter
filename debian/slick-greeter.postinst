#!/bin/sh

set -e

if [ "$1" = "configure" ]; then
  update-alternatives --install /usr/share/xgreeters/slick-greeter.desktop \
  lightdm-greeter /usr/share/xgreeters/slick-greeter.desktop 60
fi

#DEBHELPER#

exit 0
