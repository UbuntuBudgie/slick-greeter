From 3b1d30f338fdc1f9f9d4e11c3e6913edebd0ec8e Mon Sep 17 00:00:00 2001
From: Stefano Capitani <stefano@manjaro.org>
Date: Tue, 11 Jul 2017 13:43:19 +0200
Subject: [PATCH] Session detection: Add support for more DE/WM  (#61)

---
 src/slick-greeter.vala | 12 +++++++++++-
 1 file changed, 11 insertions(+), 1 deletion(-)

diff --git a/src/slick-greeter.vala b/src/slick-greeter.vala
index 70305ae..9cc62f9 100644
--- a/src/slick-greeter.vala
+++ b/src/slick-greeter.vala
@@ -183,8 +183,18 @@ public class SlickGreeter
         sessions.append ("plasma");
         sessions.append ("kde-plasma");
         sessions.append ("kde");
+        sessions.append ("budgie-desktop");
         sessions.append ("gnome");
-
+        sessions.append ("LXDE");
+        sessions.append ("lxqt");
+        sessions.append ("pekwm");
+        sessions.append ("pantheon");
+        sessions.append ("i3");
+        sessions.append ("enlightenment");
+        sessions.append ("deepin");
+        sessions.append ("openbox");
+        sessions.append ("awesome");
+        
         foreach (string session in sessions) {
             var path = Path.build_filename  ("/usr/share/xsessions/", session.concat(".desktop"), null);
             if (FileUtils.test (path, FileTest.EXISTS)) {
-- 
2.11.0

